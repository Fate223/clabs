#include <stdio.h>
#define MAXLINE 50
main()
{
    int x,b,c;
    b=c=0;
    x = getchar();
    while (x="a"&&"A"){
        b++;
    }
    while (x=0){
        c++;
    }
    printf("the number of a is :%u\n",b);
    printf("the number of 0 is :%u\n",c);
}